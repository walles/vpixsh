grammar;

pub Commandline: Vec<String> = {
    Words,
}

Words: Vec<String> = {
    <mut l:Words> " " <r:Word> => {l.push(r.to_string()); l },
    <w:Word> => vec![w.to_string()],
};

Word: String = <w:r"[^ ]+"> => w.to_string();
